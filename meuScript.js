let primeironum = parseInt(window.prompt ("Insira o Primeiro Número"));
let segundonum = parseInt(window.prompt ("Insira o Segundo Número"));

if (primeironum > segundonum) {
    alert(primeironum + " é o maior")
} else {
    alert(segundonum + " é o maior")
}
